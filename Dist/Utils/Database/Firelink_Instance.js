"use strict";
/*import {Firelink} from "mobx-firelink";
import type {RootState} from "../../Store";
import type {FirebaseDBShape} from "../../Store/firebase";
console.log("TestB.1");
// we have to put this in a separate file, so that we can "guard it behind an import" from Firelink.ts, so that we can ensure it gets initialized prior to the code (and imports!) of Firelink_Init.js (which rely on it already being initialized)
export let firelink_instance_internal_do_not_import = new Firelink<RootState, FirebaseDBShape>();
console.log("TestB.2");*/ 
//# sourceMappingURL=Firelink_Instance.js.map